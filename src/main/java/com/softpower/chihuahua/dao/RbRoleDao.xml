<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.softpower.chihuahua.dao.RbRoleDao">

	<select id="findOne" resultType="com.softpower.chihuahua.entity.RbRole">
		SELECT * FROM rb_role WHERE id = #{id}
	</select>
	
	<!-- ==================== -->
	<!-- Below SQL for custom -->
	<!-- ==================== -->
	
	<select id="findByUserId" resultType="com.softpower.chihuahua.entity.RbRole">
		SELECT r.*
		  FROM rb_role r
		  JOIN rb_user_role ur ON (r.id = ur.role_id)
		 WHERE ur.user_id = #{userId}
	</select>
	
	<select id="findByRoleId" resultType="com.softpower.chihuahua.entity.RbRole">
		SELECT r.*
		  FROM rb_role r
		  JOIN rb_user_role ur ON (r.id = ur.role_id)
		 WHERE ur.role_id = #{roleId}
	</select>
	
	<select id="findByUserIdAndRoleId" resultType="com.softpower.chihuahua.entity.RbRole">
		SELECT r.*
		  FROM rb_role r
		  JOIN rb_user_role ur ON (r.id = ur.role_id)
		 WHERE ur.user_id = #{userId}
		   AND ur.role_id = #{roleId}
	</select>
	
</mapper>
